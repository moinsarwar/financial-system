import {useEffect,useState} from 'react';import api from '../api/client';import {Link} from 'react-router-dom';
export default function Dashboard(){const [d,setD]=useState(null);useEffect(()=>{api.get('/dashboard').then(r=>setD(r.data))},[]);if(!d)return <p>Loading…</p>;return <><div className="stats">{[['Total',d.total],['In progress',d.in_progress],['Successful',d.successful],['Rejected',d.rejected],['Unread',d.unread_messages]].map(x=><div className="stat" key={x[0]}><span>{x[0]}</span><b>{x[1]}</b></div>)}</div><div className="card"><h2>Application origination</h2><p>Product-specific workflows, structured information requests, document controls, per-user message receipts and immutable status history.</p><Link className="button" to="/applications/new">Create application</Link></div></>}
