import {useState} from 'react';import {useAuth} from '../context/AuthContext';
export default function Login(){const {login}=useAuth();const [u,setU]=useState('admin'),[p,setP]=useState('Admin123!'),[e,setE]=useState('');return <div className="login"><form onSubmit={async x=>{x.preventDefault();try{await login(u,p)}catch{setE('Invalid credentials')}}}><h1>FinVault</h1><p>Financial application origination</p><input value={u} onChange={x=>setU(x.target.value)} placeholder="Username"/><input type="password" value={p} onChange={x=>setP(x.target.value)} placeholder="Password"/><button>Sign in</button>{e&&<div className="error">{e}</div>}<small>Admin: admin / Admin123!<br/>Applicant: ahmed / User123!</small></form></div>}
