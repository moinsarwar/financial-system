apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: financial-system
spec:
  selector:
    app: audit-service
  ports:
  - port: 8007
    targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: financial-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      containers:
      - name: audit-service
        image: financial-system/audit-service:5.2
        imagePullPolicy: IfNotPresent
        env:
        - name: REDIS_URL
          value: redis://:redis123@redis:6379
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: kafka:9092
        - name: POSTGRES_HOST
          value: postgres
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: financial-system-secrets
              key: postgres-password
        - name: S3_BUCKET
          valueFrom:
            secretKeyRef:
              name: financial-system-secrets
              key: s3-bucket
        ports:
        - containerPort: 8000
