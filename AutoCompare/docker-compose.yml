services:
  web:
    image: nginx:alpine
    container_name: autocompare_web
    ports:
      - "9016:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
